{
    // Connection strings section for database connections.
    "ConnectionStrings": {
        // Connection string for the DbContext named "BethanysPieShopDbContextConnection."
        "BethanysPieShopDbContextConnection": "Server=(localdb)\\mssqllocaldb;Database=BethanysPieShop8854;Trusted_Connection=True;MultipleActiveResultSets=true"
    },

    // Logging configuration section.
    "Logging": {
        // Log level settings, specifying different log levels for different categories.
        "LogLevel": {
            // Default log level for all categories is set to "Information."
            "Default": "Information",

            // Log level for the "Microsoft.AspNetCore" category is set to "Warning."
            "Microsoft.AspNetCore": "Warning"
        }
    },

    // Allowed hosts configuration section.
    "AllowedHosts": "*"
}
